% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{evinf_pmap}
\alias{evinf_pmap}
\title{Run a function over a list in parallel, with a progress bar}
\usage{
evinf_pmap(
  .x,
  .f,
  ...,
  seed,
  label = "bootstrap",
  verbose = FALSE,
  chunk_size = 1L
)
}
\arguments{
\item{.x}{A list or vector to iterate over.}

\item{.f}{A function applied to each element of \code{.x} (plus \code{...}).}

\item{...}{Additional arguments passed on to \code{.f} on every call.}

\item{seed}{An integer seed. \code{furrr} derives an independent L'Ecuyer
stream for each element from it, identical regardless of the number of
workers or the chunk size.}

\item{label}{Progress-bar message.}

\item{verbose}{If \code{TRUE}, always show a progress bar. Otherwise one is
shown only when the user has enabled a global \pkg{progressr} handler.}

\item{chunk_size}{Number of elements handed to a worker at a time.}
}
\value{
A list, the element-wise results of \code{.f}.
}
\description{
The single point through which every parallel loop in the package goes.
Wraps \code{furrr::future_map()} with reproducible per-element RNG streams
(\code{seed}) and a \pkg{progressr} progress bar that updates once per
element.
}
\keyword{internal}
