% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_grid.R
\name{predict_grid}
\alias{predict_grid}
\title{Build a prediction grid and evaluate a model over it}
\usage{
predict_grid(
  object,
  variable,
  values = NULL,
  n = 50,
  at = list(),
  fixed = c("mean", "median"),
  type = c("states", "harmonic", "quantile", "counts", "pareto_alpha"),
  quantile = NULL,
  confint = FALSE,
  conf_level = 0.9
)
}
\arguments{
\item{object}{A fitted \code{evzinb} / \code{evinb} model.}

\item{variable}{Name of the covariate to vary.}

\item{values}{Optional explicit values for \code{variable}; otherwise an
evenly spaced sequence (numeric) or all levels (factor).}

\item{n}{Number of grid points for a numeric \code{variable}.}

\item{at}{Named list of values at which to hold specific other covariates.}

\item{fixed}{How to hold the remaining numeric covariates: \code{"mean"} or
\code{"median"}.}

\item{type}{Prediction type: \code{"states"}, \code{"harmonic"},
\code{"quantile"}, \code{"counts"} or \code{"pareto_alpha"}.}

\item{quantile}{Quantile for \code{type = "quantile"}.}

\item{confint}{Add bootstrap confidence intervals (not for \code{"states"}).}

\item{conf_level}{Confidence level.}
}
\value{
A tibble with columns \code{variable}, \code{value}, \code{type},
  \code{estimate}, \code{conf.low}, \code{conf.high} (the last two \code{NA}
  when \code{confint = FALSE}).
}
\description{
Varies one covariate over a range (or set of values) while holding the others
at representative values, and returns the model predictions in long form -
the raw material for an effect plot.
}
\examples{
\donttest{
data(genevzinb2)
model <- evzinb(y ~ x1 + x2 + x3, data = genevzinb2, n_bootstraps = 5)
predict_grid(model, "x1", type = "harmonic")
}

\dontrun{
data(hks)
hks_mod <- evzinb(osvAll ~ troopLag + lntpop + brv_AllLag,
                  data = hks, n_bootstraps = 5, multicore = FALSE)
predict_grid(hks_mod, "troopLag", type = "states")
}
}
